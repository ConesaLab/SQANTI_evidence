# Dry-run fixture: training.mode=mixed, prediction.mode=full. Paths are relative to the repo root.
project:
  genome: "tests/dryrun/data/genome.fa"
  input: "tests/dryrun/data/reads.fastq"
  outdir: "tests/dryrun/results/mixed_full"
  toolsdir: "tests/dryrun/tools"
training:
  mode: "mixed"
  lineage: "dummy_odb12"
prediction:
  species: "dryrun_species"
  mode: "full"
  utr: False
curation:
  mode: "placebo"
evaluation:
  reference_gtf: "tests/dryrun/data/reference.gtf"
