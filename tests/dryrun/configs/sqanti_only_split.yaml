# Dry-run fixture: training.mode=sqanti_only, prediction.mode=split. Paths are relative to the repo root.
project:
  genome: "tests/dryrun/data/genome.fa"
  input: "tests/dryrun/data/reads.fastq"
  outdir: "tests/dryrun/results/sqanti_only_split"
  toolsdir: "tests/dryrun/tools"
training:
  mode: "sqanti_only"
  lineage: "dummy_odb12"
prediction:
  species: "dryrun_species"
  mode: "split"
  utr: False
curation:
  mode: "placebo"
evaluation:
  reference_gtf: "tests/dryrun/data/reference.gtf"
